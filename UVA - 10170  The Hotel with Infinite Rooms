#include <stdio.h>

int main() {
    long long S, D;

    while (scanf("%lld %lld", &S, &D) != EOF) {
        long long sum = 0;

        while (sum < D) {
            sum += S;
            S++;
        }

        printf("%lld\n", S - 1);
    }

    return 0;
}
